# Dépendances
node_modules/

# Logs
*.log

# Fichiers système OS
.DS_Store
Thumbs.db
desktop.ini

# Éditeurs / IDE
.vscode/
.idea/

# Secrets — NE JAMAIS COMMITTER (mots de passe, tokens, clés privées)
.env
.env.*
*.key
*.pem
secrets.*

node_modules/
package.json
package-lock.json
_shot.js
_home_top.png

# Artefacts de TEST (captures/screenshots/previews générés par les scripts de diag)
# — préfixe « _ » réservé aux outils de dev, jamais publié.
_*.js
_*.json
_*.png
_*.html
# === À AJOUTER à ton .gitignore (ou renomme ce fichier en .gitignore s'il n'existe pas) ===

# Sources d'icônes Phosphor : INUTILES au runtime (la banque sunmates-icons-v2.js
# embarque déjà les SVG inline). On les garde sur le disque pour de futurs restylages,
# mais on ne les pousse pas (sinon +18 000 fichiers dans le repo).
SVGs/
SVGs Flat/
PNGs/
Fonts/

# Artefacts de génération / QA de la banque d'icônes
batch_*.json
out_*.json
icons_merged.json
icons_bank_v3.json
icon_master.json
phos_map.json
ICON_SPEC_v2.md
ICON_REDO_HINTS.md
ICON_REDO2_HINTS.md
ICON_REDO3_HINTS.md
qa_*.png
qa_*.svg
index.html.SAVE-icons

# Previews de design (optionnel : retire ces lignes si tu veux les versionner)
icones-preview.html
icones-contexte.html
icones-modes.html

# Ménage v570 : artefacts locaux (scratch dev archivé + captures de vérif) — gardés sur disque, hors repo
_archive/
_da_shots/

# Sauvegardes / fichiers de travail (jamais publiés) — ex. index.html.bak-tdz, preview.html.bak-css
*.bak
*.bak-*
*.SAVE*
*.orig
*.tmp

# Creds de test / scripts QA connectés (NE JAMAIS publier) — visual.js contient un compte de test
test/visual.js
test/invest/
test/shots/

# Photos privées partagees par Maxime (briefs) — hors repo public Pages
WhatsApp Image*.jpeg
WhatsApp Image*.jpg
